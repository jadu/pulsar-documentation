{% extends 'docs.html.twig' %}

{% set heading = 'Navigation' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Navigation', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/navigation/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
