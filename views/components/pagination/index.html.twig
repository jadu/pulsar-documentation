{% extends 'docs.html.twig' %}

{% set heading = 'Pagination' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Pagination', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/pagination/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
