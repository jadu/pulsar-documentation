{% extends 'docs.html.twig' %}

{% set heading = 'Loading Spinner' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Components', 'data-toggle-nav': '#components'},
        {'label': 'Loading Spinner', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/loading-spinner/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
