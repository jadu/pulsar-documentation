{% extends 'docs.html.twig' %}

{% set heading = 'Popover' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Components', 'data-toggle-nav': '#components'},
        {'label': 'Popover', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/popover/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
