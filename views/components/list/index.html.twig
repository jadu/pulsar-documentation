{% extends 'docs.html.twig' %}

{% set heading = 'List' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'List', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/list/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
