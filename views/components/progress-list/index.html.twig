{% extends 'docs.html.twig' %}

{% set heading = 'Progress list' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Progress list', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/progress-list/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
