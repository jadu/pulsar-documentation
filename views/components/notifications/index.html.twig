{% extends 'docs.html.twig' %}

{% set heading = 'Notifications' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Notifications', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/notifications/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
