{% extends 'docs.html.twig' %}

{% set heading = 'Remove button' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Components', 'data-toggle-nav': '#components'},
        {'label': 'Remove button', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/remove-button/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
