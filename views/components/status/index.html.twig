{% extends 'docs.html.twig' %}

{% set heading = 'Status' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Status', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/status/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
