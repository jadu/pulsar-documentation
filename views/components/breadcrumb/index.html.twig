{% extends 'docs.html.twig' %}

{% set heading = 'Breadcrumb' %}

{%
    set breadcrumbs = [
        {'label': 'Components', 'href': '#'},
        {'label': 'Breadcrumb', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'components/breadcrumb/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
