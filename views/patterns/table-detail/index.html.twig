{% extends 'docs.html.twig' %}

{% set heading = 'Table detail' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'href': '#'},
        {'label': 'Table detail', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/table-detail/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
