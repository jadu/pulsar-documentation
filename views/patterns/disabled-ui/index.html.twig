{% extends 'docs.html.twig' %}

{% set heading = 'Disabled UI' %}

{%
    set breadcrumbs = [
        {'label': 'Patterns', 'href': '#'},
        {'label': 'Disabled UI', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/disabled-ui/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
