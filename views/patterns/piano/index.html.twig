{% extends 'docs.html.twig' %}

{% set heading = 'Piano' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'data-toggle-nav': '#patterns'},
        {'label': 'Piano', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/piano/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}