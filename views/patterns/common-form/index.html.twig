{% extends 'docs.html.twig' %}

{% set heading = 'Common Form' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'data-toggle-nav': '#patterns'},
        {'label': 'Common form', 'href': '#'}
    ]
%}

{% set tab_guide %}
    {% include 'patterns/common-form/guide.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "guide",
          "class" : "ui-report",
          "label" : "Guide",
          "src"   : tab_guide,
          "active": true
        }
    ]
%}
