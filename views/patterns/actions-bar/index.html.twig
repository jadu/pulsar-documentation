{% extends 'docs.html.twig' %}

{% set heading = 'Actions bar' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'data-toggle-nav': '#patterns'},
        {'label': 'Actions bar', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/actions-bar/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
