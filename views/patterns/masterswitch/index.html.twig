{% extends 'docs.html.twig' %}

{% set heading = 'Masterswitch' %}

{%
    set breadcrumbs = [
        {'label': 'Patterns', 'href': '#'},
        {'label': 'Masterswitch', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/masterswitch/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
