{% extends 'docs.html.twig' %}

{% set heading = 'Rules' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'data-toggle-nav': '#patterns'},
        {'label': 'Rules', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/rules/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
