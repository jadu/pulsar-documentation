{% extends 'docs.html.twig' %}

{% set heading = 'Filter bar' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Patterns', 'data-toggle-nav': '#patterns'},
        {'label': 'Filter bar', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'patterns/filter-bar/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
