{% extends 'docs.html.twig' %}

{% set heading = 'Compound' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Compound', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'forms/compound/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
        }
    ]
%}
