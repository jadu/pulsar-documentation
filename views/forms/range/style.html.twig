{% extends '@pulsar/pulsar/components/tab.html.twig' %}
{% import 'helpers/example.html.twig' as docs %}

{% block tab_content %}
<div class="report">
    {{ docs.heading('h2', 'Validation') }}
    {{ docs.heading('h3', 'Error') }}

    {{ docs.example({
        screenreader: '50, my label, slider. Something went wrong',
        twig: "
{{
    form.range({
        'label': 'My label',
        'id': 'range-b1',
        'name': 'range-b1',
        'error': 'Something went wrong'
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Multiple Errors') }}

    {{ docs.example({
        screenreader: '50, my label, slider. Something went wrong something else went wrong',
        twig: "
{{
    form.range({
        'label': 'My label',
        'id': 'range-b2',
        'name': 'range-b2',
        'error': [
            'Something went wrong',
            'Something else went wrong'
        ]
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Error and Help') }}

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about expected input Something went wrong',
        twig: "
{{
    form.range({
        'label': 'My label',
        'id': 'range-b3',
        'name': 'range-b3',
        'error': 'Something went wrong',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'States') }}
    {{ docs.heading('h3', 'Success') }}

    <p>Highlight positive actions or indicate that something has successfully saved with the <code>has-success</code> class.</p>

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'class': 'has-success',
        'id': 'range-b5',
        'name': 'range-b5',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Warning') }}

    <p>Highlight states which may need extra attention from the user, but which aren’t quite errors, with the <code>has-warning</code> class.</p>

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'class': 'has-warning',
        'id': 'range-b6',
        'name': 'range-b6',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Danger') }}

    <p>The <code>has--error</code> class is automatically added when using the <code>error</code> option.</p>

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'class': 'has-error',
        'id': 'range-b7',
        'name': 'range-b7',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Changed') }}

    <p>Highlight actions which may have changed depending on other choices elsewhere in the user interface using the <code>has-changed</code> class.</p>

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'class': 'has-changed',
        'id': 'range-b4',
        'name': 'range-b4',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Variations') }}
    {{ docs.heading('h3', 'Help') }}

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'id': 'range-b8',
        'name': 'range-b8',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h3', 'Required') }}

    {{ docs.example({
        screenreader: '50, My label star, required, slider. Help text to give more information about expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'id': 'range-b9',
        'name': 'range-b9',
        'required': true,
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Alignment') }}
    {{ docs.heading('h2', 'Hide Label') }}

    {{ docs.example({
        screenreader: '50, my label, slider. Help text to give more information about the expected input.',
        twig: "
{{
    form.range({
        'label': 'My label',
        'show-label': false,
        'id': 'range-b11',
        'name': 'range-b11',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Long Label Behaviour') }}

    {{ docs.example({
        screenreader: '50, my label, slider. A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline. Help text to give more information about expected input.',
        twig: "
{{
    form.range({
        'label': 'A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline',
        'id': 'range-b12',
        'name': 'range-b12',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Top Labels') }}

    <p>You should avoid using really long labels, but if you have to, it may be more appropriate to use the <code>form__group--top</code> class to give it more space.</p>

    {{ docs.example({
        screenreader: '50, my label, slider. A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline. Help text to give more information about expected input.',
        twig: "
{{
    form.range({
        'label': 'A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline',
        'class': 'form__group--top',
        'id': 'range-b13',
        'name': 'range-b13',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Flush Inputs') }}

    <p>Depending on the flow of your user interface you may want to position your inputs along the left edge of the UI</p>

    {{ docs.example({
        screenreader: '50, my label, slider. A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline. Help text to give more information about expected input.',
        twig: "
{{
    form.range({
        'label': 'A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline',
        'class': 'form__group--flush',
        'id': 'range-b14',
        'name': 'range-b14',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

    {{ docs.heading('h2', 'Flush Inputs & Top Label') }}

    {{ docs.example({
        screenreader: '50, my label, slider. A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline. Help text to give more information about expected input.',
        twig: "
{{
    form.range({
        'label': 'A longer than expected label which will probably wrap multiple lines and push the following input onto the next baseline',
        'class': 'form__group--top form__group--flush',
        'id': 'range-b15',
        'name': 'range-b15',
        'help': 'Help text to give more information about expected input'
    })
}}
    " }) }}

</div>
{% endblock tab_content %}
{% block tab_sidebar %}
    {{ docs.toc() }}
{% endblock tab_sidebar %}