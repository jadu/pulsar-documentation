{% extends 'docs.html.twig' %}

{% set heading = 'Creating a form' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Creating a form', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'forms/creating-a-form/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
        }
    ]
%}
