{% extends 'docs.html.twig' %}

{% set heading = 'Content' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Content', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'forms/content/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
        }
    ]
%}
