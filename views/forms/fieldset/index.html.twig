{% extends 'docs.html.twig' %}

{% set heading = 'Fieldset' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Fieldset', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'forms/fieldset/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true
        }
    ]
%}
