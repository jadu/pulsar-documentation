{% extends 'docs.html.twig' %}

{% set heading = 'Reset Button' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Symfony Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Reset Button', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'symfony-forms/reset-button/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true 
        }
    ]
%}
