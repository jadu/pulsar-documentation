{% extends 'docs.html.twig' %}

{% set heading = 'Submit Button' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Symfony Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Submit Button', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'symfony-forms/submit-button/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true 
        }
    ]
%}
