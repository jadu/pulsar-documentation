{% extends '@pulsar/pulsar/components/tab.html.twig' %}
{% import 'helpers/example.html.twig' as docs %}

{% block tab_content %}
<div class="report">
    <p class="lede">Creates a single radio button.<br /><br />For a complete reference you should refer to the <a href="https://symfony.com/doc/3.4/reference/forms/types/radio.html">Symfony RadioType documentation</a>.</p>

    <p>If the radio button is selected, the field will be set to the specified value. Radio buttons cannot be unchecked - the value only changes when another radio button with the same name gets checked.</p>

    <p>The Radio type isn't usually used directly. More commonly it's used internally by other types such as the Choice type. If you want to have a boolean field, use a checkbox instead.</p>

    {{ docs.heading('h2', 'Dependencies') }}

    <p>Your form builder will need to include the <a href="https://symfony.com/doc/3.4/reference/forms/types/radio.html">RadioType</a>.</p>

<pre><code lang="php">use Symfony\Component\Form\Extension\Core\Type\RadioType;
</code></pre>

    {{ docs.heading('h2', 'Basic usage') }}

<pre><code lang="php">$builder->add('basic', RadioType::class, [
    'label' => 'Checkbox field',
    'required' => false
]);</code></pre>

    {{ docs.heading('h2', 'Options') }}

    <table class="table table--full">
        <tbody>
            <tr>
                <td>Inherited options</td>
                <td>
                    <ul>
                        <li>data</li>
                        <li>disabled</li>
                        <li>label</li>
                        <li>required</li>
                    </ul>
                </td>
            </tr>
            <tr>
                <td>Pulsar attributes</td>
                <td>
                    <ul>
                        <li>attr.data-group-class</li>
                        <li>attr.data-guidance-html</li>
                        <li>attr.data-guidance-text</li>
                        <li>attr.data-help-html</li>
                        <li>attr.data-help-text</li>
                    </ul>
                </td>
            </tr>
        </tbody>
    </table>

    {{ docs.heading('h2', 'Inherited options') }}
    {{ docs.heading('h3', 'Data (force selected on load)') }}

<dl>
    <dt>Type:</dt>
    <dd><code>mixed</code></dd>
    <dt>Default:</dt>
    <dd><code>Defaults to field of the underlying structure</code></dd>
</dl>

<p>To make a checkbox checked by default, use the <code>data</code> option.</p>

<pre><code lang="php">$builder->add('checked', RadioType::class, [
    'label' => 'Checkbox field',
    'required' => false,
    'data' => 'value'
]);</code></pre>

    {{ docs.heading('h3', 'Disabled') }}

<dl>
    <dt>Type:</dt>
    <dd><code>boolean</code></dd>
    <dt>Default:</dt>
    <dd><code>false</code></dd>
</dl>

<p>If you don't want a user to modify the value of a field, you can set the disabled option to true. Any submitted value will be ignored.</p>

<pre><code lang="php">$builder->add('checked', RadioType::class, [
    'label' => 'Checkbox field',
    'required' => false,
    'disabled' => true
]);</code></pre>

    {{ docs.heading('h3', 'Label') }}

<dl>
    <dt>Type:</dt>
    <dd><code>string</code></dd>
    <dt>Default:</dt>
    <dd>The label is "guessed" from the field name</dd>
</dl>

<p>Sets the label that will be used when rendering the field. Setting to false will suppress the label.</p>

<pre><code lang="php">$builder->add('myLabelExample', RadioType::class, [
    'label' => 'My Label',
    'required' => false
]);</code></pre>

</div>
{% endblock tab_content %}
{% block tab_sidebar %}
    <h4>On this page</h4>
    <ul class="page-nav">
        <li><a href="#dependencies">Dependencies</a></li>
        <li><a href="#basic-usage">Basic usage</a></li>
        <li><a href="#options">Options</a></li>
        <li><a href="#inherited-options">Inherited options</a>
            <ul>
                <li><a href="#data-(force-selected-on-load)">Data</a></li>
                <li><a href="#disabled">Disabled</a></li>
                <li><a href="#label">Label</a></li>
            </ul>
        </li>
    </ul>
{% endblock tab_sidebar %}
