{% extends 'docs.html.twig' %}

{% set heading = 'Number' %}

{%
    set breadcrumbs = [
        {'label': 'Home', 'href': '/'},
        {'label': 'Symfony Forms', 'data-toggle-nav': '#forms'},
        {'label': 'Number', 'href': '#'}
    ]
%}

{% set tab_code %}
    {% include 'symfony-forms/number/code.html.twig' %}
{% endset %}

{%
    set tabs_content = [
        {
          "id"    : "code",
          "class" : "ui-report",
          "label" : "Code",
          "src"   : tab_code,
          "active": true 
        }
    ]
%}
